name = "quickbet-prediction-market"
compatibility_date = "2024-01-01"

# Pages configuration
pages_build_output_dir = "dist"

[env.production]
name = "quickbet-prediction-market"
routes = [
  { pattern = "quickbet.pages.dev", custom_domain = true }
]

# Environment variables (configure in Cloudflare dashboard)
[env.production.vars]
NODE_ENV = "production"
